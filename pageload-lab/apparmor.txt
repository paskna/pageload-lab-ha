#include <tunables/global>

profile pageload_lab flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  network inet stream,
  network inet6 stream,
  network unix stream,

  /opt/pageload-lab/** r,
  /usr/** rix,
  /lib/** rix,
  /lib64/** rix,
  /bin/** rix,
  /sbin/** rix,
  /etc/** r,
  /proc/** r,
  /sys/devices/system/cpu/** r,
  /dev/null rw,
  /dev/urandom r,
  /dev/shm/** rwk,
  /tmp/** rwk,
  /data/** rwk,

  capability setgid,
  capability setuid,
  capability chown,
  capability dac_override,
  capability fowner,
  capability sys_chroot,

  signal (send,receive) peer=pageload_lab,
}
